// Import required modules
import {
  detectBaseLanguage,
  generateTravelPlan,
  calculateCarbonFootprint,
  translateMessage,
} from "./firestoreFunctions";
import {
  generateJsonContent,
  generateStreamContent,
  generateText,
} from "./httpsFunctions";

// Export functions
export {
  generateTravelPlan,
  calculateCarbonFootprint,
  translateMessage,
  detectBaseLanguage,
  generateJsonContent,
  generateStreamContent,
  generateText,
};
